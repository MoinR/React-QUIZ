import React from 'react';
import logo from '../logo.svg';

const Loader = () => {
    return (
        <div className="align-middle loader">
            <img src={logo} className="App-logo" alt="logo" />
        </div>
    );
}
export default Loader; 